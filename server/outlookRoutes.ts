import { Express } from "express";

/**
 * Outlook routes — OAuth flow removed in favour of iCal feed.
 * This file is kept as a stub so the import in _core/index.ts still resolves.
 */
export function registerOutlookRoutes(_app: Express): void {
  // No routes needed for iCal-based integration
}
